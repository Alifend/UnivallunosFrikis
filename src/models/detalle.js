export default class Detalle {
    constructor(factura, servicio, cantidad, precio) {
        this.factura = factura;
        this.servicio = servicio;
        this.cantidad = cantidad;
        this.precio = precio;
    }
  }